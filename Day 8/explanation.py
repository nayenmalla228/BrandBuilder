"""🔹 Explanation of Parts:
Part	                Meaning
try	                    Code you try to run.
except	                Code that runs if there's an error.
else	                Runs only if no error happens.
finally	                Always runs (even if there's an error). Useful for cleanup."""